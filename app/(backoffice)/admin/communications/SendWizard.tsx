// Placeholder — replaced in Task 8 with the bulk-send wizard (upload/parse,
// column mapping, preview, saved-list creation).
export default function SendWizard(_props: { presetListId: string | null; clearPreset: () => void }) {
  return null;
}
