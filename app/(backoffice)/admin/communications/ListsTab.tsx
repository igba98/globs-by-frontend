// Placeholder — replaced in Task 8 with the saved contact lists table
// (view/delete list, "Send to this list" action).
export default function ListsTab(_props: { onSendToList: (id: string) => void }) {
  return null;
}
